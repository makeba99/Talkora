You are a senior frontend engineer tasked with improving the UI/UX of a Connect2Talk language practice community application. Your goal is to implement the following fixes and features across the application, ensuring responsive design and visual consistency with the existing dark, space-themed design system.

**Core Tasks:**

1. **Fix Scrollbar Design Consistency**
   - Replace the current non-themed scrollbar with one that matches the application's dark theme and neon/gradient aesthetic
   - Ensure the scrollbar is properly styled on all screen sizes and doesn't obscure interactive elements (particularly the "Create Room" button)
   - Test on smaller screens to verify the scrollbar doesn't interfere with UI components

2. **Responsive "Create Room" Display**
   - Ensure the "Create Room" button displays fully and remains accessible on smaller screens
   - Adjust layout/spacing so no elements are hidden or truncated on mobile and tablet viewports
   - Reference the "Create a Voice Room" modal design (image.png) for styling consistency

3. **Add Fake Card Groups on Home Page**
   - Create multiple groups of room cards (similar to the existing grid shown in image.png)
   - Each group should contain cards with different topics/languages
   - Each card should include: room title, language/flag, difficulty level, participant count, user profile pictures, placeholder user icons, "Join Spot" option, and "Step In" button
   - These card groups should persist on the home page (remain visible when users navigate and return)

4. **Add Teacher Section**
   - Create a dedicated "teacher section" in the application
   - Populate it with teacher profile cards following the same design language as the room cards
   - Ensure teacher profiles are visually consistent with the existing neon-bordered, gradient-accented card design

**Design Requirements:**
- Maintain the dark, space-themed background with vibrant neon/gradient highlights
- Use the existing card design pattern (rounded corners, neon borders, gradient accents)
- Reference the "Card Theme" color palette from the modal (image.png shows the available gradient options)
- Ensure all new elements are responsive and function properly on smaller screens, with 3 cards fully visible per row on smaller viewports

**Testing:**
- Verify all components render correctly on mobile, tablet, and desktop breakpoints
- Confirm the scrollbar styling is applied consistently across all scrollable areas
- Test that persistent card groups remain in place during navigation