You are a full-stack developer tasked with fixing three critical issues on a room-sharing platform. Your goal is to deliver production-ready solutions that directly address user experience and admin functionality problems.

**Issue 1: User Profile Pictures Not Visible**

Profile pictures are currently missing or broken for users (as shown in image.png, where "Evraksee" displays a broken image placeholder). Users cannot see profile pictures on user cards or in profile displays.

Fix this by:
- Identifying why profile picture URLs are broken or not loading (check image storage paths, CDN configuration, or database references)
- Ensuring profile pictures display correctly on all user cards and profile pages
- Testing that pictures are visible to all users, not just the account owner
- Verify the fix works across the platform interface

**Issue 2: Shorten Room URLs**

Current room URLs are too long:
`https://www.afikgang.online/room/25605e5e-094d-41f6-8059-0e91483402c6`

They should be shortened to:
`https://www.afikgang.online/room/iw002?key=757791`

Where:
- `iw002` is a short, unique room identifier (visible to the host only)
- `757791` is the API key parameter (visible to the host only)
- The full UUID should remain in the backend but not exposed in the user-facing URL

Implement URL shortening that maintains security—ensure that shortened URLs are unique, non-guessable, and the key is required for access.

**Issue 3: Platform Owner Can Delete User Accounts**

Add functionality to the platform owner's admin dashboard that allows the platform owner to delete user accounts from the system.

After deletion:
- The user account should be completely removed from the platform
- Ensure all associated data is handled appropriately (decide whether to soft-delete or hard-delete based on your data retention policy)
- Provide confirmation before deletion to prevent accidents

Deliver all three fixes production-ready with proper error handling and testing.