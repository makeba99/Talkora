You are a senior web performance engineer. Your mission is to optimize this web app to achieve Lighthouse Performance score = 100 (mobile + desktop).

⚠️ STRICT RULES

DO NOT change UI, layout, animations, or functionality
DO NOT remove features
ONLY optimize loading, execution, and delivery
Preserve pixel-perfect rendering
🎯 CORE OBJECTIVES
Achieve:
LCP < 1.8s
TBT ≈ 0ms
CLS = 0
JS execution minimal
Reduce total payload from ~3.5MB → < 1MB
Eliminate render-blocking + unused code
🚀 CRITICAL OPTIMIZATIONS (EXECUTE ALL)
1. 🧠 JavaScript Optimization
Enable aggressive code splitting
Route-based splitting (lazy load pages like lobby, rooms, etc.)
Convert all non-critical components to dynamic imports
Remove unused JS (target: eliminate ~85KB unused)
Tree-shake all dependencies
Replace heavy libraries with lighter alternatives where possible (NO UI change)
Ensure production build uses:
minification
dead code elimination
module concatenation
2. 📦 Bundle Control
Split vendor bundle (react-vendor) into smaller chunks
Ensure main bundle < 150KB
Enable gzip + brotli compression
Analyze bundle and remove duplicate dependencies
3. 🖼️ Image Optimization (BIGGEST WIN)

Current issue: ~2.8MB image payload

Convert all images → WebP or AVIF
Implement:
loading="lazy" for all below-the-fold images
responsive srcset
Resize images to exact display dimensions
Cache images aggressively (CDN)
Replace proxy image system with optimized delivery
Preload ONLY the LCP image
4. ⚡ Network & Request Optimization
Reduce critical request chain (currently 442ms)
Inline critical CSS (above-the-fold)
Defer all non-critical JS
Remove duplicate requests (JS/CSS shown twice)
Enable HTTP/2 or HTTP/3
5. 🌐 Preconnect & Resource Hints

REMOVE unused preconnect:

<link rel="preconnect" href="https://flagcdn.com">
Add preconnect ONLY for actual critical origins (max 2–3)
Use:
dns-prefetch for minor third-party domains
6. 🎨 CSS Optimization
Remove unused CSS
Minify CSS
Split critical vs non-critical CSS
Load non-critical styles asynchronously
7. 🔤 Font Optimization
Preload main font

Use:

font-display: swap;
Subset fonts (latin only if applicable)
Ensure font file < 20KB
8. 🧱 Rendering Performance
Eliminate layout shifts:
Always define width/height for images/videos
Avoid forced reflows
Use transform instead of layout-triggering properties
9. ⚙️ Caching Strategy

Enable long-term caching:

cache-control: public, max-age=31536000, immutable
Use hashed filenames for cache busting
10. 🚫 Third-Party Control
Remove or lazy-load all non-critical third-party scripts
Ensure zero render-blocking external scripts
📊 VALIDATION

After optimization:

Run Lighthouse (mobile + desktop)
Must hit:
Performance: 100
No unused JS warnings
No large payload warnings
No render-blocking resources
🧠 PRIORITY ORDER
Image optimization (biggest gain)
Code splitting + lazy loading
Remove unused JS/CSS
Reduce bundle size
Fix network chain + duplicates
✅ OUTPUT REQUIREMENT
Apply all optimizations directly in code
Keep UI EXACTLY the same
Ensure app behavior is unchanged
Deliver production-ready optimized build