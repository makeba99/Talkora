You are an expert UI/UX developer specializing in dark-mode design systems and component theming. I have uploaded two images — **image 1** (current theme) and **image 2** (target reference theme). My goal is to transform the current UI theme to closely match the aesthetic of the second image, applied consistently across every page and component including the chat panel, navigation bar, buttons, and all interactive elements.

**Current theme (image 1):** Dark brown/maroon main content area with dark blue/black nav and chat panel. Buttons are a mix of solid red/purple circles, light blue utility icons, and pill-shaped status tags in purple and orange.

**Target theme (image 2):** Deep charcoal/black background with a subtle granular texture. Cyan/light blue as the primary accent color for active states, the Send button, language tags, and highlights. Circular control buttons are dark grey with white icons; active/warning states use red fills. The Send button is a rounded rectangle with a solid cyan fill. Clean sans-serif white/light-grey typography throughout.

Apply the following changes:

1. **Global background:** Replace the brown/maroon and dark blue tones with a unified deep charcoal/black (`#0d0d0d` or equivalent) with a subtle granular or noise texture overlay.

2. **Accent color system:** Replace all purple, orange, and light-blue accent instances with a single cyan/teal accent (`#00BCD4` or close match from image 2). Apply this to: active tab indicators, the Send button fill, language/status tag fills, focus rings, and hover states.

3. **Button redesign:**
   - Circular control buttons (mute, camera, screen share, raise hand): dark grey background (`#2a2a2a`) with white icons; keep red fill only for active warning states (e.g., muted)
   - The **Send button**: rounded rectangle, solid cyan fill, white text/icon — replace the current light blue version
   - Navigation/utility icons: white or light grey, no colored backgrounds unless active

4. **Status/pill tags:** Restyle "French," "Host," and similar tags to use the cyan accent with white text, removing the current purple/orange palette.

5. **Chat panel:** Apply the same charcoal background, cyan accents, and updated button styles — no separate color treatment for the sidebar.

6. **Typography:** Ensure all text uses clean sans-serif, white or light-grey (`#e0e0e0`) on dark backgrounds — consistent across nav, chat, and content areas.

7. **Consistency:** The theme must be applied globally — every page, modal, panel, and state should follow this system. No component should retain the old brown, maroon, purple, or orange palette.

Provide the complete updated CSS (or theme configuration file, depending on the stack) needed to implement these changes, including variable definitions, component-level overrides, and any texture/background implementation. If the codebase uses a theming system (CSS variables, Tailwind config, styled-components theme, etc.), structure the output accordingly.