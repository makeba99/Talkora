You are a world-class web performance engineer.

Your mission: Refactor and optimize this application to achieve:

* 💯 100/100 Performance (Desktop AND Mobile)
* 💯 100/100 Accessibility
* 💯 100/100 Best Practices
* 💯 100/100 SEO

🚨 NON-NEGOTIABLE CONSTRAINT:

* ZERO visual changes (pixel-identical UI)
* ZERO functionality changes
* ZERO UX degradation

If any optimization risks altering UI or behavior → DO NOT APPLY IT.

---

## 🎯 PRIMARY METRIC TARGETS (MANDATORY)

* LCP (Largest Contentful Paint) → < 1.5s
* TBT (Total Blocking Time) → < 100ms
* CLS → < 0.05
* INP → < 100ms

---

## 🔥 CRITICAL FIXES (PRIORITY ORDER)

1. LCP OPTIMIZATION (BIGGEST ISSUE)

* Identify the LCP element (likely main dashboard / hero / large container)
* Preload LCP asset (image/font/component)
* Ensure LCP is NOT lazy-loaded
* Inline critical CSS for LCP section
* Remove render-blocking CSS/JS affecting LCP
* Use priority loading (fetchpriority="high")

2. JAVASCRIPT (FIX TBT 660ms → <100ms)

* Aggressively split bundles (route + component level)
* Convert all non-critical components to dynamic import()
* Defer ALL non-essential scripts
* Eliminate unused JS completely
* Replace heavy libraries with lighter alternatives ONLY if identical behavior
* Move expensive logic off main thread (Web Workers if safe)

3. HYDRATION / REACT OPTIMIZATION (if applicable)

* Avoid full hydration for non-critical UI
* Memoize components (React.memo, useMemo, useCallback)
* Prevent unnecessary re-renders
* Use partial hydration or islands if possible

4. CSS OPTIMIZATION

* Remove unused CSS safely
* Inline ONLY above-the-fold CSS
* Defer the rest
* Avoid large CSS frameworks if unused portions exist

5. IMAGE OPTIMIZATION

* Convert to AVIF/WebP (NO visible quality loss)
* Add width + height (fix CLS)
* Use responsive srcset
* Lazy load ONLY below-the-fold images

6. CLS FIX (0.142 → <0.05)

* Reserve exact layout space for:

  * images
  * avatars
  * dynamic cards
* Prevent late DOM injection shifts
* Use skeleton loaders matching final layout EXACTLY

7. FONT OPTIMIZATION

* Preload critical fonts
* Use font-display: swap WITHOUT layout shift
* Limit font variants

8. NETWORK + DELIVERY

* Enable Brotli compression
* Reduce total requests
* Use HTTP/2 or HTTP/3
* Add preconnect to critical origins

9. THIRD-PARTY CONTROL

* Defer or lazy load ALL third-party scripts
* Remove unnecessary integrations

---

## ⚡ MOBILE-FIRST OPTIMIZATION (CRITICAL)

* Reduce JS payload drastically (<150KB ideal)
* Avoid heavy animations on mobile
* Ensure fast interaction readiness
* Optimize for slow CPU + network

---

## ♿ ACCESSIBILITY (KEEP 100)

* Fix remaining issues WITHOUT UI changes
* Add missing labels, roles, alt text
* Ensure keyboard navigation works perfectly

---

## 🧪 VALIDATION (MANDATORY)

* UI must be pixel-identical before vs after
* All features must function exactly the same
* No console errors
* No hydration issues
* No layout shifts introduced

---

## 📦 OUTPUT FORMAT

1. Identify EXACT causes of:

   * High LCP
   * High TBT
   * CLS issues

2. Show BEFORE → AFTER code

3. Explain impact of each fix:

   * Which metric it improves
   * Expected score gain

4. Provide final expected PageSpeed scores:

   * Desktop
   * Mobile

FINAL GOAL:
Achieve TRUE 100/100 across ALL PageSpeed categories on BOTH Desktop and Mobile WITHOUT altering design or functionality.
