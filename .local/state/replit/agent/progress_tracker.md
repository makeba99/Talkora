[x] 1. Install the required packages
[x] 2. Restart the workflow to see if the project is working
[x] 3. Verify the project is working using the feedback tool
[x] 4. Inform user the import is completed and they can start building, mark the import as completed using the complete_project_import tool
[x] 5. Redesign the floating control panel - standardized muted button colors, premium spacing and styling, larger buttons, consistent color logic (only Leave is red)
[x] 6. Premium Chat & YouTube panel redesign - color-coded tabs, elevated filter bar, premium empty states, refined message bubbles with hover backgrounds, frosted glass input, full-width 16:9 video cards with gradient overlays and play button reveal
[x] 7. Side panel icon-only tab switcher row, remove duplicate panel toggles from control bar (keep only Chat + Settings), add plasma theme with purple/blue energy background gradient, chat panel glow, and animated orb overlay
[x] 8. Database performance optimization: 20+ indexes added across all tables, connection pool tuned (max 20 connections, idle/connection timeouts), getConversations rewritten from full JS memory scan to efficient SQL CTE, automated cleanup scheduler with configurable retention via env vars
[x] 9. Second wave of optimizations: gzip compression on all API responses, TTL in-memory cache for users/rooms/external APIs, fixed 4 N+1 query patterns (admin badges, blocked users, teacher reviews, bookings), YouTube/GIF results cached 5min, orphaned upload file cleanup added to scheduler
[x] 10. Ultra-secure privacy notifications: security_events DB table; helmet security headers (CSP/HSTS/Referrer-Policy/Permissions-Policy); rate limiting on auth (30/15min), API (180/min), uploads (20/hr) keyed by session not IP; XSS & SQL injection pattern detection middleware; privilege escalation detection; securityBus for real-time admin socket alerts; admin Security Events tab in /admin with severity badges, resolve workflow, live count badge; security notification types in notifications dropdown; privacy-first (no IP stored anywhere)
[x] 11. Repair broken profile picture rendering: avatar images now drop to initials fallback on image load failure, profile image paths are normalized on profile updates, uploaded images stay served from /uploads, and legacy imported avatar/image filenames can be served from the project root when referenced by older data.
[x] 12. Add short room links while preserving UUID room IDs: rooms now store a short_id and access_key, legacy UUID /room/:id URLs continue to work, lobby joins request a short /room/{shortId}?key={accessKey} link, and short-link room loading validates the key before returning the UUID room payload used by WebRTC.
[x] 13. Add owner-only account deletion: Platform Owner can permanently delete non-admin user accounts from Admin > Users, with server-side owner/admin/self-delete guards and cleanup across user messages, rooms, votes, follows, reports, notifications, bookings, teachers, badges, comments, announcements, and security-event references.
[x] 14. Fix room-card sample images and room key rules: demo participants now render generated local portrait images; public room links work without keys; private rooms reject unkeyed short/UUID access and only host/admin access-link generation returns the private keyed URL.
[x] 15. Migration complete - project running on Replit with all dependencies installed and workflow configured.
[x] 16. Adjustable restriction duration: Platform Owner can now pick any number of days (1-365) when restricting a user via a day-picker popover in Admin > Users, replacing the fixed "1 day" button. Restricted users receive a persistent DB notification (admin_restriction type). When restriction expires, the cleanup scheduler automatically clears it and sends an admin_restriction_lifted notification via DB and socket. Lifted restrictions (manual or auto) also generate a DB notification with labels and icons in the notifications dropdown.
[x] 17. Private notes on followed users: A sticky-note icon button appears next to the message icon for every user you follow in the social panel. Clicking it opens a private popover with a textarea (up to 1000 chars) that saves to the new user_notes DB table. Notes are only visible to the author. The icon turns amber when a note exists. Notes persist through the API routes GET/POST /api/notes/:subjectId.
[x] 18. Platform Owner can adjust active restriction time for any non-owner user from Admin > Users. The same restriction popover now appears for already restricted users, defaults to their remaining days, caps updates to 1-365 days, and notifies the user immediately.
[x] 19. Joining a different voice room now automatically removes the user from their previous room exactly like pressing Leave: old room participants, card counts, media states, roles, and cleanup timers update immediately.
[x] 20. Room deletion is only available inside Room Settings for hosts, with the existing confirmation flow and server-side host-only delete enforcement.
[x] 21. Private notes are available beside followed/friend users in both the main social panel and the in-room People/user controls.
[x] 22. Host Edit Room Settings now uses the same four-card theme carousel style as Create Room, including image thumbnails, arrows, selected state, and dot pagination.
[x] 23. When the same user joins another room, the previous room tab now attempts to close itself automatically; if the browser blocks tab closing, it falls back to navigating out of the old room.
[x] 24. Migration to Replit environment complete - tsx PATH issue fixed, workflow running, app serving on port 5000.
[x] 25. Theme dialog (quick-access from control panel) now shows the same 4-up image carousel with arrows and pagination dots as Edit Room Settings, instead of the old plain text grid.
[x] 26. Fixed private notes: fetch now sends credentials so the session cookie is included and notes load correctly for followed users in both the social panel and in-room people list.
[x] 27. Room tab auto-close fix: server now always emits room:joined-another-room to the previous socket (removed same-socket guard); tab always navigates to lobby after window.close() attempt so it reliably leaves even when browser blocks tab closing.
[x] 28. Theme list unified: create-room-dialog.tsx now imports ROOM_THEMES from profile-decorations.tsx (the same source used by Edit Room Settings and room rendering), removing the separate out-of-sync 19-item local array. Create Room and Edit Room Settings now show identical themes.
[x] 29. Migration fully verified - dev script updated to use local tsx binary, workflow running cleanly, app serving on port 5000.
[x] 30. Private notes now available for ALL users (no follow required): note button moved from the social panel user row into a new profile mini-dialog that opens by clicking any user's avatar or name. The dialog shows profile info, online status, note section, and follow/message/join-room actions. In-room participant card also shows note for all participants (not just followed ones). Social panel rows now only show message + follow icons, fixing the "Follow" button being cut off at the edge.
[x] 31. AI Tutor holographic face: replaced BrainCircuit icon with animated SVG anime face (girl with silver hair/blue eyes when Female, guy with dark hair/teal headphones when Male). Holographic circle has cyan glow, scan-line overlay, shine layer, and speaking ping animation. Clicking AI Tutor button no longer auto-opens settings (like mic). Chat only starts when user types. Gender/voice toggle in settings instantly changes the face. OPENAI_API_KEY confirmed present in secrets.
[x] 32. Final Replit migration step: fixed dev script to use local tsx binary (./node_modules/.bin/tsx) instead of npx tsx, eliminating the interactive install prompt that was blocking the workflow. App confirmed running on port 5000 with all API routes healthy and Vite connected.
[x] 33. AI Tutor improvements: (1) Layout shifts to flex-row when chat opens so face moves left automatically and returns to center when chat closes. (2) Animated mouth overlay on holographic face — upper/lower lip divs with mouthTalk/mouthTalkLip CSS keyframe animations that open/close naturally when speaking; mouth stays subtle/closed at rest. New face image updated. (3) Full Web Speech Recognition (SpeechRecognition API) implemented — AI listens continuously when active, pauses during AI speech to avoid feedback, restarts automatically after AI finishes speaking; recognized speech sent directly to sendAiMessage; ring glows green while listening, cyan while speaking; status shows "Listening…"/"Speaking…"/"Ready".
[x] 34. AI Tutor multi-user & session management: (1) Server-side roomAiTutorState + roomAiTutorEnabled Maps track one-at-a-time sessions per room. Socket events: room:ai-tutor-start/stop/speaking/set-enabled. (2) Client listeners for state/busy/disabled/enabled-changed with toasts and forced dismissal. (3) toggleAiTutor emits start/stop; speakAiText emits speaking true/false for all observers. (4) 5 varied context-aware intro texts per room language. (5) Chat panel now an independent fixed element (top:50%, left:calc(50%+140px)) — AI face ALWAYS centered. (6) SVG cupid's-bow mouth with svgMouthOpen/svgLipSync CSS animations positioned at 70% of face height. (7) Host-only "Room AI Access" toggle in settings panel. (8) "X is practicing with AI Tutor" observer badge shown to other participants with live speaking dot.
[x] 35. Replit migration re-verified: dependencies installed, database schema pushed, workflow configured for web preview on port 5000, app restarted successfully, browser connected to Vite, and preview/API requests return successfully.
[x] 36. Fixed YouTube and AI Tutor room behavior: security headers now allow YouTube scripts/embeds, AI Tutor answers are generated from the user's voice/text in context, and AI replies are broadcast so other room participants can see/hear the one active AI conversation.
[x] 37. YouTube reliability fix: room playback now uses a direct youtube-nocookie iframe instead of depending on the YouTube iframe API script, and the YouTube search box accepts pasted YouTube links or raw video IDs as playable results.
[x] 38. Final migration verification complete: all packages installed, database schema pushed, workflow running cleanly on port 5000, Vite connected, app serving successfully.
[x] 39. Fixed YouTube sync interruptions: removed dead guard that was disabling the entire YT.Player sync engine; replaced the uncontrolled visible iframe with the API-controlled yt-player-container div so seek/play/pause sync actually applies to what users see; increased ytRemoteAction lock from 1s to 3s to prevent buffering-after-seek from re-broadcasting as a local event. Fixed AI Tutor mic loop: added watchdog interval that polls speechSynthesis.speaking every 500ms to detect the Chrome bug where onend never fires, force-resumes paused synthesis, and restarts mic recognition when speech silently ends; also added hard timeout fallback for stuck utterances.
[x] 40. Full room responsiveness pass: control dock buttons scale w-9→w-11→w-12 across xs/sm/md breakpoints with matching icon sizes (15px→18px), labels hidden on mobile, dock gap/padding tightened; AI Tutor face uses CSS min(220px, 55vw) so it shrinks proportionally on small screens; AI broadcast bubble uses max-w-[80vw] on mobile; AI chat panel repositioned via .ai-chat-panel-responsive CSS class — centered below the face on mobile, shifted right of center on sm+ screens.
[x] 41. Fixed AI Tutor echoing user input: rewrote the /api/ai-tutor/chat OpenAI call path — added startup warning if key is missing, proper try/catch with error logging (status code + response body) around the fetch, increased max_tokens to 300; replaced the broken fallback with a pool of 6 varied natural conversation prompts chosen at random so the AI always feels responsive even if OpenAI is unavailable.
[x] 42. Fixed YouTube stopping mid-session: only the broadcaster now emits PLAYING/PAUSED sync events; added ENDED state handler so the broadcaster auto-loops the video; added playsinline: 1 to playerVars for iOS compatibility.
[x] 43. Fixed YouTube "stuck in loading" for watchers: broadcaster PLAYING events are now debounced — only synced when: (a) it's the first play, (b) the position has jumped >3s (manual seek), or (c) >8s wall-clock has passed since the last sync. Watcher side updated: incoming "play" sync only calls seekTo if drift is >3s. Pause sync always applies immediately.
[x] 44. AI Tutor full overhaul: smarter anti-repetition system prompt, real-time interim transcript shown as dashed "Heard: ..." bubble, "Got it, thinking…" acknowledgment, status label updated to Speaking/Processing/Listening/Ready states, YouTube conflict detection, repetition detection runs client-side. "Show AI Thoughts / Debug Script" toggle button in chat panel header.
[x] 45. AI model routing + chat panel default: Model routing layer with NVIDIA Nemotron Nano → gpt-4o → gpt-4o-mini → context-aware fallback. System prompt rewritten for natural conversation. Chat panel reverted to closed by default.
[x] 46. Migration to Replit complete - workflow running, app serving on port 5000, all API routes healthy, Vite connected, database connected.
[x] 47. Fixed YouTube stuck/black screen: replaced YT IFrame API (which mutated the React-managed DOM causing reconciliation conflicts) with a React-managed iframe using youtube-nocookie.com embed URL. Player control now uses the postMessage API, state changes and currentTime are tracked via window message events. All existing call sites preserved via a thin proxy object in youtubePlayerRef.
[x] 48. AI Tutor fixes: listening state no longer moves eyebrows, and invalid database-URL NVIDIA_API_KEY values are ignored instead of being sent to NVIDIA.
[x] 49. AI Tutor avatar realism pass step 1: upgraded the existing circular SVG avatar with skin micro-texture, facial depth highlights/shadows, natural layered brows, hair strand lighting, deeper iris/pupil rendering, and subtle nose/cheek contours while preserving the holographic AI Tutor frame and lip-sync overlay.
[x] 50. AI Tutor compact realism pass: default voice is locked back to Female on load, browser auto-selected voice IDs are no longer saved, female voice selection avoids common male voices, avatar options are limited to female human presets, avatar colors are more natural, and the AI overlay/chat panel are smaller and more responsive across screens.
[x] 51. AI Tutor mouth placement fix: removed the static painted smile that caused a second mouth, simplified the lower nose shadow so it no longer reads as a mouth, repositioned the viseme-driven mouth to the proper lower-face location, softened lip colors, and tuned the default female avatar toward the silver-hair/blue-eye reference.
[x] 52. AI Tutor reference-style face and listening eyes: added headset band/earpiece glow, darker high-collar suit details, extra silver hair strands, separated iris/pupil highlights into an eye-mimic group, and added listening-only eye tracking with natural blink timing while keeping brows still and the mouth closed at rest.
[x] 53. Migration fully complete - dev script updated to use npx tsx (reliable across all Replit environments), workflow running cleanly, app serving on port 5000, Vite connected, all API routes healthy.
[x] 54. Enhanced Host YouTube Sync: (1) Server-side enforcement — only room host (ownerId) can start/stop videos via room:youtube; only the broadcaster can emit room:youtube-state changes. (2) Host control panel overlay on player — play/pause button, stop button, seek slider with MM:SS time display, volume slider with mute toggle; revealed on hover, gradient backdrop. (3) Participant blocking overlay — transparent div intercepts all pointer events on the iframe, preventing non-host interaction with YouTube controls. (4) Role-split YouTube sidebar panel — host sees search bar, trending, stop button; participants see "Now Watching" card with thumbnail, broadcaster avatar, watcher count, and Join Watch Party button; "No video playing" placeholder when idle. (5) Playback state tracking (ytIsPlaying, ytCurrentTime, ytDuration, ytVolume) via 1s poll interval, with reset on stop.
[x] 55. AI Tutor avatar overhaul: replaced photo+floating-overlay system (which caused same-face-for-both-genders and misaligned mouth/eyes bugs) with fully custom inline SVG faces. Male face has dark short hair, warm skin, square jaw, thick flat brows, stubble shadow, steely blue eyes, teal headset. Female face has long silver hair with bangs, light pink skin, arched thin brows, large blue eyes with lashes, cheek blush, pink lips, purple headset. Mouth animation is now embedded directly inside the SVG at exact face coordinates — no more floating overlays. Eye blink uses ai-avatar-blink CSS class on SVG ellipse elements. Both small (corner) and large (fullscreen) avatar views updated.
[x] 56. AI Tutor photorealistic portrait upgrade: SVG faces replaced with AI-generated photorealistic portrait photos (attached_assets/ai-tutor-female.png, ai-tutor-male.png). Female: dark wavy hair, professional headset, blue-purple starfield background. Male: dark swept hair, glasses, beard, professional headset, teal-dark background. AiTutorFace component now uses <img> base + positioned SVG mouth overlay. Skin-blend radial gradient covers the original closed smile, animated viseme mouth renders on top. Component correctly imported in voice-room.tsx.
[x] 57. Migration to Replit re-verified: database schema synced, workflow running cleanly on port 5000, app serving successfully.
[x] 58. Final import verification: dependencies reinstalled, database schema pushed, workflow restarted and running cleanly on port 5000, Vite connected, all API routes healthy.
[x] 59. Re-migration verified: node_modules reinstalled, database schema re-pushed (tables restored), workflow running cleanly on port 5000, /api/rooms returning 200.
[x] 60. Migration to Replit re-verified: dependencies installed, database schema pushed, workflow restarted and running cleanly on port 5000.
