[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg"]

[phases.install]
cmds = ["npm install --registry https://registry.npmjs.org"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/index.cjs"
